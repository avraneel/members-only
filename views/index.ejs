<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Members Only</title>
    <link rel="stylesheet" href="./styles.css" />
  </head>
  <body>
    <% if (locals.user) { %>
      <h1>Welcome back, <%= user.first_name %></h1>
      <a href="/logout">Log Out</a>
    <% } %>
    <div class="block"><%- include('partials/navbar') %></div> 
    <div class="block"><%- include('partials/messages.ejs', { messages: messages }) %></div>
  </body>
</html>
